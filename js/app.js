/* Mandamos una alerta al usuario que invocara a la funcion vigenere cuyos parametros
 * seran introducidos por el usuario
 */
window.alert(vigenere(window.prompt(PEDIR_TEXTO), window.prompt(PEDIR_CLAVE)));

